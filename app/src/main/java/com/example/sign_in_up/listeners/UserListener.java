package com.example.sign_in_up.listeners;

import com.example.sign_in_up.models.User;

/**
 * Listener interface for handling user interactions in the sign-in/sign-up module.
 */
public interface UserListener {
    void onUserClicked(User user);

}
